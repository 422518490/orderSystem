server:
  port: 8091

spring:
  application:
    name: config_server
  cloud:
    bus:
      id: config_server_bus
    nacos:
      discovery:
        #配置服务注册中心服务器地址
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848

#加密盐值
encrypt:
  key: 1234abcd-

